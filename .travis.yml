dist: focal

os:
- linux

language: cpp

before_script:
- sudo apt -y install libxrandr-dev
- mkdir build
- cd build
- cmake ..
addons:
  apt:
    update: true

script:
- cmake --build build/

after_script:
	
